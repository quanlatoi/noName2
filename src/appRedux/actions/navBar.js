import * as types from '../constants/actionTypes';

export const controllMenu = ()=>{
    return {
        type: types.CONTROLL_MENU
    }
}

export const mouseHoverInMenu = ()=>{
    return {
        type: types.MOUSE_HOVER_IN_MENU
    }
}